---
name: stats
description: Show current context-os status — branch, uncommitted files, session objective
user_invocable: true
---

Run context-os status to show current project context:

```bash
context-os status
```

Show the output to the user.
