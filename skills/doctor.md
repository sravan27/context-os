---
name: doctor
description: Validate context-os setup and run a quick benchmark
user_invocable: true
---

Run the context-os doctor command to validate the setup:

```bash
context-os doctor
```

Show the output to the user. If any checks fail, suggest running `context-os init` to fix them.
