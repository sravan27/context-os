# Sample Sessions

This directory holds structured session-memory examples and trace artifacts.

- `state.json`: example persisted session-memory state
- `update.json`: example incremental update payload for the session-memory CLI
- `long-prompt.txt`: noisy prompt fixture used by the prompt linter
