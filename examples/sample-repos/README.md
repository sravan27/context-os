# Sample Repos

This directory holds license-safe fixture repositories used by repo-memory and benchmark tests.

- `mini-next/`: minimal Next.js-style fixture used by the repo-memory indexer tests and CLI smoke runs
