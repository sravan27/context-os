[project]
name = "context-os-evals"
version = "0.1.0"
description = "Evaluation runners and reporting utilities for Context OS"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
